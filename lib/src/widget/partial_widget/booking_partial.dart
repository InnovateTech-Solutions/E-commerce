import 'package:flutter/material.dart';

class BookungPartial extends StatelessWidget {
  const BookungPartial({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        children: [],
      ),
    );
  }
}
